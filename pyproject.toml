[project]
name = "dbt-ditto"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dbt-duckdb>=1.11.0",
    "dbt-loom>=0.9.6",
    "dbt-osmosis>=1.5.0",
]
